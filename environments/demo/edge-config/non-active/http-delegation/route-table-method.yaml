apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  name: 'httpbin-rt'
  namespace: 'gloo-system'
spec:
  routes:
    - matchers:
       - prefix: '/foo'
         methods:
         - GET
         #- POST
      routeAction:
        single:
          upstream:
            name: 'httpbin-httpbin-8000'
            namespace: 'gloo-system'